<div class="container">
      <div class="row">
        <div class="col-lg-8 col-md-10 mx-auto">
        <p class="text-center font-weight-bold"> Novo Comentário</p>
        <p>
          <%= form_for @comment, url: create_comment_path(@comment.post_id), remote: true do |f| %>
            <div style="display:none"><%= f.text_field :post_id %></div>
            <p>
              <%= f.text_area :content, id:'comentario-conteudo', size:'90x5', class:'form-control'  %>
            </p>
            <p><%= f.submit 'Comentar', class:'btn btn-primary', id:'comentar-botao' %></p>
            
          <% end %>
        </p>
        </div>
      </div>